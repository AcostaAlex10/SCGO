# El proyecto tiene conflictos de peers heredados de la exportación de Figma, y
# sin esto npm se niega a instalar. Estaba como flag en cada comando
# (--legacy-peer-deps); acá lo respetan también Dependabot y Vercel, que no
# saben pasar flags.
legacy-peer-deps=true
